/**
 * calcular la distancia recorrida por un carro 
 * con velocidad X en un tiempo T.
 */
const speed = prompt('Cual es la velocidad:')
const time = prompt('Cual fue el tiempo total:')

console.log('Distancia: ', speed * time)